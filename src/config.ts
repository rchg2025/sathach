// Cấu hình URL cho API
// Trong môi trường development (Vite), request sẽ được proxy qua localhost:5000 (xem vite.config.ts).
// Khi deploy lên Vercel, request sẽ được serverless function xử lý nội bộ thông qua vercel.json.
export const API_BASE_URL = '';
